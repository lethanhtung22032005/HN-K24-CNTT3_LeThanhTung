server:
  port: 8082

spring:
  application:
    name: inventory-service
  # Câu 1 + Câu 5: nạp cấu hình tập trung (datasource, jpa, kafka, redis) từ Config Server
  config:
    import: optional:configserver:http://localhost:8888
