server:
  port: 8081

spring:
  application:
    name: order-service
  # Câu 1 + Câu 5: nạp cấu hình tập trung (datasource, jpa, kafka, eureka) từ Config Server
  config:
    import: optional:configserver:http://localhost:8888
