server:
  port: 8080

spring:
  application:
    name: api-gateway
  # Câu 1: nạp cấu hình (eureka + routes) tập trung từ Config Server
  config:
    import: optional:configserver:http://localhost:8888
