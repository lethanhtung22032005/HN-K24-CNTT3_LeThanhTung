server:
  port: 8083

spring:
  application:
    name: payment-service
  # Câu 1 + Câu 5: nạp cấu hình tập trung (datasource, jpa, kafka) từ Config Server
  config:
    import: optional:configserver:http://localhost:8888
